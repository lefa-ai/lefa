{
  "name": "lefa-site",
  "private": true,
  "type": "module",
  "description": "Lefa landing page + waitlist API (Vercel).",
  "scripts": {
    "test": "vitest run --config vitest.config.js",
    "test:coverage": "vitest run --coverage --config vitest.config.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^30.0.1",
    "vitest": "^4.1.10"
  }
}
