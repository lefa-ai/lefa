{
  "name": "lefa",
  "private": true,
  "description": "Lefa — an open operating environment for AI software engineers.",
  "scripts": {
    "dev": "pnpm --filter @lefa/desktop dev",
    "build": "pnpm --filter @lefa/desktop build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
